class LibraryUser:
    def __init__(self, name, user_id):
        self.name = name
        self.user_id = user_id
        self.borrowed_books = []  # Initialize an empty list for borrowed books

    
